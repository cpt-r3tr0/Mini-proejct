<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to <%=shopName%></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.8.1/flowbite.min.css"  rel="stylesheet" />
  <link rel="stylesheet"  type="text/css" href="/main.css" />
  
</head>
<body class="flex flex-col min-h-screen">
  <header>
    <%- include('header') %> 
  </header>

  <main class="mt-4 flex-grow px-5 mx-auto max-w-6xl">

  <div class="mb-4">

  <h1 class="text-2xl font-bold mb-4">Welcome to the Berties Book Store.</h1>
  <p>One place to find all the books you desire.</p>

  </div>

  <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
    <% for (let i = 0; i < books.length; i++) { %>
      <div class="h-auto max-w-full rounded-lg overflow-hidden">
        <img class="h-auto max-w-full rounded-lg" src="<%= books[i].cover_url %>" alt="avatar">
        <div class="p-4">
          <h1 class="uppercase tracking-wide text-sm font-bold text-gray-700"><%= books[i].title %></h1>
          <div class="flex justify-between">
            <p class="text-gray-700"><%= books[i].author_name %></p>
            <p class="text-gray-700 font-bold"><%= books[i].first_publish_year %></p>
          </div>
          <p class="text-gray-700"> $<%= books[i].price %></p>
          
        </div>
      </div>
    <% } %>
  </div>

</main>
<footer><%- include('footer', { class: 'mt-auto' }) %></footer>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/1.8.1/flowbite.min.js"></script>
</body>
</html>